#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _atoi - converts a string to an integer.
 * @s: the string to be converted.
 * Return: the integer.
 */

int _atoi(char *s)
{
	int integer;

	integer = atoi(s);
	return (integer);
}
